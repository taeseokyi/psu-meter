# 인수인계 문서 (HANDOFF)

**프로젝트**: 해수 수조 광학식 염도계 (psu-meter)
**작성**: 2026-08-31 · Claude 세션 결과 정리
**대상**: 이 작업을 이어받는 사람 또는 다음 세션

이 문서는 "무엇을 만들었나"가 아니라 **"왜 이렇게 결정했고, 무엇이 아직 안 풀렸나"** 를 적은 것입니다.
결과물 목록은 [README.md](README.md), 부품은 [BOM.md](BOM.md)를 보세요.

---

## 1. 요청의 출발점

대화는 ESP32-S3 / HC-05 배선 질문에서 시작해 다음으로 이어졌습니다.

1. "아두이노로 염도를 안정적으로 측정할 수 있나" → 전기전도도(EC) 방식 설명
2. **"전기전도도 말고 광학적으로 안 되나? LED와 센서로?"** ← 프로젝트의 실질적 시작
3. 상시 침지 대신 펌프로 물을 퍼와 측정하는 구조 제안 (사용자 아이디어)
4. 측정 사이에 셀을 보관액으로 채우는 구조 제안 (사용자 아이디어)
5. 파워포인트 설계 문서 → 부품 조달 → 배선도 → 측정 헤드 조립 → 3D 프린팅 대응

**사용자 환경**: 해수 리프 수조. HC-06 블루투스 + 스마트폰 시리얼 터미널로 명령. 하루 2회 자동 측정.
**제약**: 알루미늄 절삭 가공은 불가, 3D 프린팅만 가능.

---

## 2. 핵심 아키텍처 — 이것만은 반드시 이해할 것

> **매 측정 사이클마다 RO-DI(0 PSU)와 표준액(35 PSU) 두 점으로 자가보정한다.**

이 한 줄이 설계 전체를 지탱합니다. 왜 중요한지:

| 오차원 | 절대 측정 | 차분 측정 | 비고 |
|---|---|---|---|
| 온도 — 둘이 **함께** 드리프트 | 0.667 PSU/°C | **0.0015 PSU/°C** | 450배 개선 |
| 온도 — 둘 **사이**의 차이 | 0.667 PSU/°C | **0.667 PSU/°C** | ⚠ **상쇄 안 됨.** 아래 참조 |
| LED 파장 드리프트 | 0.013 PSU/nm | 거의 0 | 프리즘 분산이 이미 상쇄 (§4) |
| LED 광량·검출기 게인 | 직접 영향 | 0 | 비율 연산으로 제거 |
| 기구 열팽창 (250 mm Al) | 0.091 PSU/°C | 거의 0 | 차분으로 상쇄 |

> **두 번째 줄이 이 설계에서 가장 위험한 항목입니다.** 자가보정이 지우는 것은
> 표준액과 시료가 *같은 온도로 함께* 움직일 때뿐입니다. 둘의 온도가 서로
> 다르면 1차 계수 0.667 PSU/°C 가 그대로 살아납니다.
> **0.1 PSU 를 지키려면 두 액의 온도차가 0.15 °C 이내여야 합니다.**
> 그보다 크면 같은 센서로 양쪽을 재고 dn/dT 로 보정해야 하고, 그때 남는 것이
> 센서 분해능 0.0625 °C × 0.667 = 0.042 PSU 입니다 (오차예산에 반영됨).

### 1단계(수동) 오차예산 — 확정 형상 (2026-09-02)

> 재현: `python src/budget_manual.py` · 잡음 한계는 `src/profile_crb.py`

**확정 형상** — 반원 60×30×10 · 허브 일체형 · **조명팔 40 mm / 검출팔 60 mm(비대칭)** ·
슬릿 0.19 mm · 부채꼴 4.5° · TSL1401CL 베어 칩 · 액적 **20 µL**
**본체 196 mm** (비대칭: 조명측 93 + 검출측 103) × **20.8** × **52** (+힌지 귀 7) · 받침대 220 × 70 × 6 · 측정면 높이 **49 mm**

> **★ 2026-09-02 정정 — 팔의 기준점.** 두 곳이 서로 다르게 잡고 있었습니다.
> `src/segment_optics.py` 의 팔은 **측정점 M 부터**이고(ARM_MM 주석 그대로),
> 기구 설계(`docs/head.html`·`scad/`)의 팔은 **곡면 통과점 E 부터**입니다.
> 반원은 곡면 굴절이 없어 광선이 M 을 지나는 하나의 직선이므로
> **광학 지렛대 = E·주광선(= R = 30) + 기계팔** 입니다.
> 기계팔 60 mm 로 만든 물건의 지렛대는 60 이 아니라 **90** 이고,
> 그래서 실제 성능은 문서보다 1.5배 좋습니다 — 스팬 9.0 → **13.5 px**,
> 3.88 → **2.59 PSU/px**, 칩 1 µm 0.061 → **0.041 PSU**.
> 같은 오류로 조명 패치 계수(`16.51×…`)도 틀려 있었습니다 — 그 계수는
> **활꼴 60×20 @ 팔 250** 에서 맞춘 값이라 반원에 안 맞습니다. 패치는
> 5.9 가 아니라 **8.7 mm**, 액적은 11 이 아니라 **20 µL** 입니다.
> 이제 `docs/head.html` 이 계수를 버리고 **조명 광선추적을 직접** 합니다.
>
> ⚠ **남은 위험 하나** — 주광선을 수조 염도(34 PSU)에 조준해 두어서
> **RO-DI(0 PSU) 경계가 조명 부채꼴 가장자리에 0.046° 여유로 걸칩니다**
> (35 PSU 쪽은 0.561°). 조준을 17.5 PSU 로 옮기면 양쪽 0.30° 로 균형이
> 맞고 팔 각도는 26.152° → **26.411°** 로 0.26° 만 변합니다.
> **2026-09-02 에 옮겼습니다** — 여유가 0.305° / 0.302° 로 균형이 잡혔습니다.
> 두 팔 사이각도 127.695° → **127.177°** 로 바뀌었습니다.

> **2026-09-02 정정 — 블록 깊이.** 옛 값 42(→ 본체 45 · 측정면 43)는 프리즘만 보고
> 정한 것이라 **검출 채널이 L = 59.8 mm 에서 블록 바닥을 뚫고 나갔습니다. 칩이 바로
> L = 60 mm** 입니다(칩 아래 살 −0.07 mm). 팔이 26.41° 내려가므로 깊이는 **팔 끝**이
> 정하고, 최심점은 칩 포켓(어레이 방향 ±4.7 mm)이라 **49** 가 됩니다.
> [`docs/head.html`](docs/head.html) 의 **"블록 깊이 검사"** 행이 이제 자동 판정합니다.
> 같은 날 `scad/optical_bench.scad` 를 일체형으로 **전면 재작성**하고 OpenSCAD
> 2025.09.07 로 렌더·STL 검증했습니다.

> **본체 폭은 부품 적재까지 세어야 나옵니다.** 슬릿은 조명팔 끝에 있어야 하고
> LED·확산판이 그 **뒤**에 오므로 조명측은 팔 40 + **21 mm**, 검출측은 칩·배선으로
> 팔 60 + **12 mm** 입니다. 좌우를 따로 세지 않으면 조용히 모자랍니다 —
> 실제로 대칭으로 잡았던 초안이 검출측에서 10 mm 부족했습니다.
> [`docs/head.html`](docs/head.html) 의 "부품 적재 검사" 행이 좌우를 각각 판정합니다.

> **팔이 비대칭인 이유** — 조명팔은 **액적 크기**가 정합니다(조명 패치 ∝ 조명팔 × 부채꼴).
> 검출팔은 **화소당 PSU 와 기계 안정성**이 정합니다. 두 요구가 서로 반대이므로
> 한쪽만 늘리는 것이 맞습니다. 기하 번짐이 `슬릿 × (검출/조명)` 으로 1.5배 커지지만
> 번짐은 거의 무해합니다(§4 "경계 폭 모델의 한계").

| 항목 | 값 [PSU] | 비고 |
|---|---|---|
| 잡음 (16프레임, SNR 1300) | 0.0027 | 데이터시트 1 mV rms + 10비트 ADC |
| **PRNU** (플랫필드 잔차 0.2 %) | **0.0222** | 보정 없으면 **0.44** — 플랫필드는 필수 |
| 2점 보정 — 곡선 피팅 후 잔차 | 0.0050 | **직선 그대로면 0.0837** |
| **온도** (√2 × 0.0625 °C × 0.667) | **0.0590** | **최대 항목.** 액적마다 2회 읽기 → √2 (§6-1) |
| **증발** (뚜껑 닫기까지 편차 2 초) | **0.0193** | **증기 웰**로 뚜껑 닫으면 멎는다. 남는 것은 열린 시간 편차 (§6-1) |
| **제곱합** | **0.0658** | |
| 미광·표면굴곡·기계드리프트 여유 | **0.1004** | 목표 0.12 에서 남는 것 |

**직선 보정을 그대로 쓰면 제곱합이 0.0961 로 올라가고 여유가 0.072 밖에 안 남습니다** —
미광과 표면 굴곡이 들어갈 자리가 없어 사실상 실패합니다.

#### (4) 기계 안정성 — 짧은 검출팔의 진짜 대가 (2026-09-02 추가)

예산에서 빠져 있던 항목입니다. 스팬이 화소 몇 개뿐이면 **칩이 프리즘에 대해 옆으로
조금 미끄러지는 것**이 그대로 염도 오차가 됩니다.

| 검출팔 [mm] | 스팬 [px] | PSU/px | 1 µm 이동 [PSU] | 0.05 PSU 허용 이동 |
|---|---|---|---|---|
| 40 | 6.0 | 5.82 | 0.092 | **0.55 µm** |
| 50 | 7.5 | 4.66 | 0.073 | 0.68 µm |
| **60 (지렛대 90) ★확정** | **13.5** | **2.59** | **0.041** | **1.23 µm** |
| 80 | 12.0 | 2.91 | 0.046 | 1.09 µm |
| 100 | 15.0 | 2.33 | 0.037 | 1.36 µm |

**한정 두 개.**

- **절대 안정성이 아니라 보정과 시료 측정 사이(1분)의 상대 이동**입니다.
- **균일 열팽창은 무해합니다.** 본체가 통째로 커지면 각도는 안 변하고 배율만 변해서
  PLA 70 ppm/°C 로도 **0.0024 PSU/°C** 입니다. 위험한 것은 **비대칭 이동**입니다.

> ⚠ **그래서 뚜껑에 조건이 붙습니다.** 액적을 누르려고 뚜껑을 밀면 그 힘이 프리즘–칩 사이
> 본체를 휘게 하고, 누르는 세기가 매번 다르면 그게 곧 비반복 오차입니다.
> **뚜껑은 허브 귀에 M3 로 달린 작은 판**입니다 (2026-09-04. 그 전에는 바닥판에
> 내려앉는 슈라우드였습니다 — §4 참조). 허브에 달리므로 하중 경로가 생겼고,
> 그래서 **래치를 달지 않고**(자중 약 0.2 N) **피벗을 조이지 않습니다**(유격 0.4).
> 배선 슬롯이 열린 양 끝면은 슬롯을 y 로 꺾어서 + 검정 실런트로 막습니다.

#### 이 예산에서 나온 요구사항 세 개

**(1) 2점 보정을 직선으로 하지 말 것.** 0 과 35 PSU 를 직선으로 이으면 중간(17.5 PSU)에서
**0.0837 PSU** 틀립니다. 그리고 이 값은 **팔 길이와 무관**합니다(30·40·60·100·160 mm 전부 동일) —
순수하게 직선 근사 탓입니다. 응답 곡선은 `detector_pos()` 로 계산되므로 **알려진 곡선을 피팅하면
사라집니다.** 예산에서 가장 큰 항목이 펌웨어로 공짜로 없어집니다.

**(2) 플랫필드 잔차를 0.2 % 까지.** PRNU ±4 % 를 그냥 두면 **0.63 PSU** 로 목표의 5배입니다.
팔이 짧을수록 화소당 PSU 가 커져 레버리지가 커집니다 (40 mm 에서 5.82 PSU/px).

| 팔 [mm] | PSU/px | 보정 없음 | 플랫 0.4 % | 플랫 0.2 % |
|---|---|---|---|---|
| 30 | 7.76 | 0.706 | 0.071 | 0.035 |
| **40** | **5.82** | **0.629** | **0.063** | **0.032** |
| 60 | 3.88 | 0.443 | 0.044 | 0.022 |
| 100 | 2.33 | 0.352 | 0.035 | 0.018 |

**(3) 온도 센서가 최대 항목이 됐습니다.** DS18B20 12비트(0.0625 °C)를 액적마다 2회 읽어
차분하므로 √2 × 0.0625 × 0.667 = 0.0590 PSU 입니다 (§6-1).
0.01 °C 급으로 올리면 0.0067 PSU 가 되어 제곱합이 0.033 까지 내려갑니다.
수동 측정은 여기서 유리합니다 — 같은 방의 두 액을 1분 안에 연달아 재면 온도차가 작습니다.

---

**파생 결론 세 가지** — 이걸 모르면 설계를 잘못 수정하게 됩니다.

**(a) 프리즘을 자유롭게 바꿔도 된다.** 검량선의 기울기·절편을 매번 실측하므로 프리즘의 정확한 굴절률·형상·검출팔 길이를 미리 알 필요가 없습니다. 계산값은 "감도가 충분한가, 두 보정점이 128 px 안에 들어오는가"만 확인하는 용도입니다. → **값싼 직각/정삼각 프리즘으로 먼저 시작해도 됩니다.**

**(b) 기구 정밀도 요구가 낮다.** 그래서 3D 프린팅이 가능합니다. 거울로 광로를 접어도 됩니다.

**(c) 온도 센서의 절대 정확도가 필요 없다.** 같은 센서로 표준액과 시료를 재고 차이만 쓰므로 절대 오차는 상쇄됩니다. 필요한 건 분해능뿐이고, DS18B20의 0.0625 °C면 0.042 PSU 수준입니다.

**전제 조건**: 표준액과 시료가 **같은 온도**여야 합니다. 이게 깨지면 위 표가 전부 무의미해집니다.
셀 유로 안에서 두 액의 온도를 각각 재고 dn/dT로 보정하는 것이 (c)의 실제 내용이며,
"온도는 상쇄되니 신경 안 써도 된다"로 읽으면 안 됩니다. §8의 미해결 항목 "수조물의 온도 도달"이
바로 이 조건이 지켜지는지의 문제입니다.

---

## 3. 주요 설계 결정과 그 이유

각 항목은 "이걸 바꾸려는 사람"이 왜 그러면 안 되는지 알 수 있게 적었습니다.

### 광학 방식 — 임계각(critical angle)

요구 분해능이 **굴절률의 1/14,800** (±0.5 PSU 기준 Δn ≈ 9.1×10⁻⁵)입니다. 흡광도 방식은 소금이 가시광을 거의 흡수하지 않아 불가능하고, 프리즘 편각 방식은 Δn에 대한 변위가 너무 작아 광학 벤치 길이가 비현실적입니다. 임계각 방식만 남습니다. 상용 디지털 굴절계와 같은 원리입니다.

### 광원 — 단색 LED 590 nm (백색 절대 불가)

> **2026-09-01 2차 정정.** 아래 수치는 프리즘 분산을 넣어 다시 계산한 값입니다.
> 옛 값(1 nm = 0.19 PSU)은 시료 분산만 보고 프리즘 분산을 빼먹은 것이라 **14배 과대**였습니다.
> 자세한 것은 §4를 보십시오. **파장 1 nm = 0.013 PSU** 입니다.

| 광원 | 스펙트럼 폭 | 경계 번짐 | (옛 계산) |
|---|---|---|---|
| 백색 LED | ~150 nm | **2.0 PSU** — 34 ± 2 PSU 창을 덮음, 사용 불가 | 28 PSU |
| 단색 LED | ~20 nm | 0.26 PSU | 3.8 PSU |
| 레이저 다이오드 | <1 nm | 0.01 PSU (단 스페클 노이즈) | 0.2 PSU |

**결론은 그대로입니다** — 백색 LED의 2.0 PSU 번짐은 리프 수조가 머무는 34 ± 2 PSU 창을
그대로 덮으므로 여전히 못 씁니다.

**달라지는 것은 발주 조건입니다.** 반치폭 40 nm 짜리 싸구려 앰버 LED라도 0.52 PSU 이므로,
"반치폭 20 nm 이하를 데이터시트로 확인할 것"은 더 이상 필수 조건이 아닙니다.
§5의 미확인 항목에서 이 건을 내렸습니다.

### 프리즘 — BK7 (저굴절이 오히려 유리)

| 유리 | 임계각 | 감도 |
|---|---|---|
| BK7 n=1.5168 | 61.93° | **253 µrad/PSU** |
| SF계 n=1.72 | 51.09° | 167 µrad/PSU (−34 %) |

임계각이 클수록 cos θc가 작아져 각도 변화가 증폭됩니다. 비싼 고굴절 유리가 불리합니다.
형상은 반원통(hemicylinder)이 이상적 — 곡면으로 들어온 빛이 중심을 향하면 굴절 없이 통과해 광로 계산이 단순합니다.

### 검출기 — TSL1401 리니어 어레이 (2분할 포토다이오드 아님)

처음엔 2분할 포토다이오드 차동 방식을 검토했으나 기각했습니다.
**RO-DI 보정점이 34 PSU 밖에 있어서 2분할 검출기의 선형 구간(약 ±2 PSU)으로는 볼 수 없습니다.** 1점 보정만 가능해지고, 그러면 오프셋 드리프트는 잡아도 **감도(기울기) 드리프트를 못 잡습니다.**

TCD1304(고해상도 리니어 CCD)도 기각 — 1~4 MHz ADC가 필요해 아두이노로는 불가능합니다. 실제로 Arduino 포럼에 ESP8266으로 200 kHz밖에 안 나와 실패한 사례가 있습니다. TSL1401은 클럭이 훨씬 느려 나노로도 충분합니다.

### 측정 위치 — 수조 밖 플로우셀 (상시 침지 아님)

수조에 광학면을 담그면 며칠 만에 생물막이 앉습니다. 임계각 측정은 표면에서 파장 이하(수백 nm) 두께만 보기 때문에 **눈에 안 보이는 얇은 막도 그대로 오차**가 됩니다. 해양 계류 광학 센서 분야의 오랜 난제이고, 상용 장비는 와이퍼·구리 차폐·UVC 같은 방오 기구를 따로 답니다.

→ 펌프로 필요할 때만 물을 퍼와 재고, 유휴 시에는 셀을 표준액으로 채워둡니다.

### 보관액 — 35.0 PSU 표준액 (RO-DI 아님)

잔류액의 굴절률 차이가 그대로 오차입니다. 오차 ≈ `잔류율 × (보관액 염도 − 34)`.

| 보관액 | 1 % 잔류 시 오차 |
|---|---|
| RO-DI (0 PSU) | 0.34 PSU |
| 35 PSU 표준액 | **0.01 PSU** |

**34배 차이**입니다. 게다가 유휴 셀이 늘 알려진 표준액으로 차 있으니 매 사이클 직전 보정점이 공짜로 생기고, 수조로 넘어가도 그냥 해수라 무해합니다.

셀을 비워 말리는 것은 **금지** — 해수가 마르면 염 결정이 남아 측정면을 망칩니다.

### 펌프 — 페리스탈틱 4개 (밸브 아님)

정지 시 튜브가 눌려 **사이펀이 원천 차단**됩니다. 수조에 연결된 장치에서 이건 타협 대상이 아닙니다. 자흡이 되고 액이 튜브 안에만 있어 교차오염도 없습니다.
예산이 문제면 밸브 + 펌프 1개로 줄일 수 있지만 침수 대책을 따로 세워야 합니다.

### 오손 방지 — UVC LED (화학약품 아님)

표백제는 미량이라도 수조 생물을 죽입니다. 자동 루프 안에 두면 안 됩니다.
UVC LED는 소모품이 없고 수조 유입 위험이 원천적으로 없습니다. 단 **UVC는 실리콘·PMMA·접착제를 열화**시키므로 근처는 PTFE나 유리를 쓰고, 프리즘 접착에 UV 경화 접착제는 금지입니다.
탄산칼슘 스케일은 UVC로 안 되므로 월 1회 묽은 구연산 순환을 따로 넣습니다.

### 상용 모듈을 쓰지 않는 이유 (2026-09-01 3차 조사)

"이미 조립된 모듈이 있지 않나"는 당연한 질문이고, **있습니다.** 그런데 둘 다 안 맞습니다.

**Vaisala K-Patents PR-33-AC** — 이름 그대로 "Sanitary **OEM** Refractometer" 이고
구조가 이 설계와 거의 같습니다. 프리즘 · **LED 590 nm** · 온도센서 · **3648 픽셀 CCD** 가
하나의 CORE-optics 덩어리이고, 단면도를 보면 프리즘 끝에서 부채꼴이 퍼져 리니어 CCD 에
바로 닿습니다 — 검출팔에 렌즈가 없는 구조입니다 ([`ILLUMINATION.md`](ILLUMINATION.md) §7).

| | nD | PSU 환산 |
|---|---|---|
| PR-33-AC 정확도 | ±0.0002 | **±1.11** |
| PR-33-AC 재현성 | ±0.0001 | **±0.55** |
| Pyxis RT-50 | ±0.2 % Brix | **±1.65** |
| **이 프로젝트 목표** | ±2.2×10⁻⁵ | **±0.124** |

**(1) 정밀도가 4.5~9배 부족합니다.** 2점 자가보정을 붙여 절대 정확도를 무시해도
재현성 0.55 PSU 가 남아 목표에 못 미칩니다. → **모듈 구매로는 목표를 못 맞춥니다.**

**(2) 전부 상시 침지형입니다.** 설치 사진을 보면 스테인리스 배관에 클램프로 박혀 있고,
RT-50 은 유로 147 mm · Ø25.4 삽입형입니다. 이건 위 "측정 위치" 항목에서 생물막 때문에
명시적으로 기각한 아키텍처입니다. 산업 공정은 유속이 크고 CIP 세정을 하니 성립하지만
수조는 아닙니다.

**(3) 수조 시장에는 자동 광학식이 아예 없습니다.** 자동 측정은 전부 전기전도도
(Neptune Apex PM2 등)이고, 광학식은 전부 손으로 보는 휴대용입니다.
**"필요할 때만 재는 광학식 전자동"은 시판 제품이 없습니다.**

#### 왜 자작이 상용 모듈보다 분해능이 좋은가 — 역설이 아니다

PR-33-AC 는 Ø110 mm 하우징 안에 다 들어가야 해서 **검출팔이 수십 mm** 입니다.
그래서 3648 픽셀(8 µm 피치)이 필요합니다. 이 프로젝트는 **검출팔 250 mm** 를 쓸 수 있어
128 픽셀(63.5 µm)로도 1 px/PSU 가 나옵니다.

→ **상용 프로브가 지불할 수 없는 것(부피)을 지불해 분해능을 사는 구조**입니다.
수조 옆 상자는 500 × 150 mm 여도 괜찮으니까요. 설계가 순진한 게 아니라, 산업용이 쓸 수
없는 자유도를 쓰고 있습니다.

#### 빌려온 것

- **Cargille 표준 굴절액** — PR-33-AC 의 교정 물질. 어차피 재는 것이 굴절률이므로
  표준해수일 필요가 없습니다. 오차예산 1위 항목(표준액 0.1 PSU)을 개선할 여지 → BOM §4
- **공기 중 교정** — RT-50 매뉴얼 §6.1 "굴절률 교정은 필요 없다. 다만 공기 중 교정으로
  광학 부품의 정렬 이탈과 노화를 보정할 수 있다". 이 설계의 공기 풀스케일 기준점과 동일 ✓
- **modified PTFE 프리즘 개스킷** (PR-33-AC) — 재질 선택 확증
- **590 nm** — 파장 선택의 독립적 확증
- **짧은 팔 + 촘촘한 화소** — 하우징이 부담되면 갈 수 있는 대안. 단 TCD1304 의 MHz
  샘플링 문제는 그대로 남습니다 (위 "검출기" 항목)

**가격은 여전히 미확인입니다.** 둘 다 견적 문의형이고 공개 가격이 없습니다.

### 3D 프린팅 — 광학면에서 좌우 클램셸 2분할

경사진 보어는 FDM으로 못 뚫습니다. **광선이 지나는 평면에서 두 쪽으로 가르면** 각 쪽에 반원 홈이 위를 향한 채 남아, 서포트 없이 매끈한 채널이 나옵니다.
250 mm 팔은 출력하면 휘므로 허브에서 각도만 잡고 검은 파이프를 끼웁니다. O-링 홈은 FDM 정밀도가 안 나오니 **1 mm 실리콘 시트 평면 가스켓**으로 대체합니다.

---

## 4. 발견해서 고친 오류

### 2026-09-04 — 설계를 뜯어보다 드러난 것 다섯

**① 포켓이 프리즘을 0.35 mm 떨어뜨리고 있었다** (수정 완료)

포켓은 반지름 `P_R + FIT` 의 원통이고 중심이 `z = 0` 이었습니다. 그 안에 실물(반지름
`P_R`)이 중력으로 앉으면 중심이 `z = −FIT` 로 내려갑니다. **측정면이 설계 평면보다
0.35 mm 아래**입니다. 결과가 둘입니다.

- 도면과 주석이 여러 곳에서 주장하던 **"창 밖 살이 프리즘을 위에서 물어 준다"가
  사실이 아니었습니다.** 살과 프리즘 사이에 0.35 mm 가 통째로 비어 있었습니다.
  누름쇠를 없앤 근거였고, 실링의 기준면이기도 했습니다.
- 광선이 프리즘 중심을 0.35 빗나가므로 곡면 입사각이 0.668°, **굴절 편향이 0.220°**
  입니다. 조명 부채꼴 여유가 편측 0.30° 뿐입니다. 2점 보정이 상수로 흡수하지만
  여유의 3분의 2를 그냥 먹습니다.

포켓 원통만 `FIT − P_SEAT` 올려 앉는 높이를 정했습니다. 남긴 `P_SEAT = 0.15` 는
프리즘 반지름 공차를 먹는 몫입니다 — 0 으로 하면 실물이 조금만 커도 프리즘이
포켓 바닥과 살 사이에 끼여 응력을 받습니다(§3 의 금기). 잔여 편향은 0.094° 이고,
없애려면 팔의 조준점을 `(0, −P_SEAT)` 로 옮겨야 합니다 — **열려 있는 문제**입니다.
검사는 `PART="prismcheck"` (앉은 실물 ∩ 본체 = 비어야 정상).

**② 분할면 Y=0 이 젖은 창을 가로지른다** (수정 완료)

창은 y 로 −4~+4 였고 분할면은 y=0 입니다. 즉 **A/B 이음매가 창 한가운데를 지나갑니다.**
그 크랙은 창 끝벽에서 시작해 프리즘 상면을 덮은 살 속을 따라 프리즘 끝(x=±30)까지
가고, 거기서 포켓으로 — 즉 **광 입출사 곡면으로** 열립니다. 곡면이 젖으면 닦을 수도
마를 수도 없습니다. 게다가 크랙은 모세관이라 20 µL 액적을 그냥 마십니다.
블록 윗면에 열린 **실런트 주입 슬롯**(`SEAM_X = ±10.5`)으로 끊었습니다.

**③ 블록 두께를 포켓만 보고 정했다** (수정 완료)

`HUB_Y = P_THICK + 2*FIT + 2*WALL = 17.7` 은 포켓 기준입니다. 창은 25° 로 벌어지므로
블록 윗면에서 창 반폭이 `WIN_HY + HUB_TOP·tan25` 가 되고, 창을 프리즘 전폭까지 넓히면
살이 **1.65 mm** 밖에 안 남습니다. 둘 중 큰 쪽으로 유도하도록 고쳤습니다 (→ 20.80).
⚠ **`thincheck` 는 Y=0 단면만 보므로 두께 방향 살은 거기 안 걸립니다.** 이 항목은
검사가 아니라 유도값 옆의 `assert` 가 지킵니다. y 방향 치수를 새로 넣을 때는
반드시 assert 를 하나 더 쓰십시오.

**④ 조명 플러그 안이 막혀 있었다 — 유산지에서 날까지 빛이 못 갔다** (수정 완료)

플러그 안의 빛의 길은 `LED → 공동 7×7 → 유산지(확산판) → 날 2장의 틈(슬릿) → 광창 →
프리즘` 입니다. 그런데 **광창 윗면 1.6 과 확산판 슬롯 아랫면 3.05 를 따로 정해 놓고
대조한 적이 없어서, 그 사이 0.925 mm 가 통째로 살로 남아 있었습니다.** 확산광이 날까지
오지 못하므로 **슬릿에서 나오는 빛이 아예 없습니다** — 1순위 검증이 그대로 실패합니다.

부피로 확인했습니다 : 슬릿 자리를 지나는 0.4 × 6 기둥과 플러그의 교집합이 옛 설계에서
**2.22 mm³**(= 0.4 × 6 × 0.925) 였고, 지금은 0 입니다.

통로를 뚫되 폭을 **x 로 ±1.5 mm** 로 좁혀 날 슬롯의 천장을 `|x| > 1.5` 에 남겼습니다.
천장이 없으면 날이 기울고, 슬릿의 평행도가 이 부품의 전부입니다. 슬릿에서 확산판을
±51° 로 보므로 부채꼴 4.5° 에는 넘치게 넉넉합니다.
z 자리를 순서대로 쌓고(`BLADE_Z0 → BLADE_Z1 → PASS_Z → DIFF_Z0 → DIFF_Z1 → LED_Z`)
assert 세 개로 이었습니다. 새 검사 : `PART="raycheck"` (비어야 정상).

> ⚠ **이 버그는 렌더도 `thincheck` 도 멀쩡히 통과했습니다.** 매니폴드이고 얇은 살도
> 아니기 때문입니다. **"빛이 지나가야 하는 길"은 형상 검사로 안 잡힙니다** — 광로마다
> 관통 검사(raycheck 같은)를 따로 두십시오.

**⑤ "권장 슬릿 0.19" 가 기계팔로 계산한 값이었다** (2026-09-04 정정)

`docs/head.html` 의 권장 슬릿이 `√(λ · 기계 검출팔 60)` = 0.19 였습니다.
**2026-09-02 의 "기계팔 ≠ 광학 지렛대" 정정이 이 줄에는 닿지 않았습니다** —
스팬과 조리개는 고쳤는데 이 한 줄이 남았습니다. 반원이라 광선이 M 을 지나는
직선이므로 제대로 잡으면 :

- a = R + 조명팔 = **70** (슬릿 → M), b = R + 검출팔 = **90** (M → 칩)
- 기하 번짐 = 슬릿 × b/a, 회절 번짐 ≈ λ(a+b)/슬릿
- 둘이 같아지는 최적 = √(λ(a+b)·a/b) = **0.27 mm**

**다만 번짐 최소가 곧 σ 최소는 아닙니다.** 칩에서의 총 번짐과 CRB σ :

| 슬릿 | 번짐 | σ (읽기잡음 지배) | σ (광자잡음 지배) | |
|---|---|---|---|---|
| 0.20 | 8.5 px | 1.41 | 1.21 | 복사용지 2장 |
| **0.27** | **7.8 px** (최소) | 1.00 | 1.00 | 번짐 최적 |
| **0.30** | 7.8 px | 0.90 | 0.95 | **`SLIT_W` 기본값** |
| 0.38 | 8.6 px | 0.75 | 0.89 | 9 mm 커터날 |
| 0.45 | 9.7 px | 0.67 | 0.87 | |
| 0.50 | 10.6 px | 0.63 | 0.86 | 18 mm 커터날 |

**★ 번짐 최소 = σ 최소가 아닙니다.** 슬릿을 넓히면 번짐과 함께 **광량도** 늘기
때문입니다. CRB 는 σ ∝ σ_n·√(번짐)/진폭 이고, 기하 영역에서 진폭 ∝ 슬릿,
번짐 ∝ 슬릿이므로 **σ ∝ σ_n/√슬릿 — 넓힐수록 좋아집니다.**
즉 **0.45 도 "너무 크지" 않습니다.** (거친 모형입니다. 정확한 판정은
`src/profile_crb.py` 와 §6 스윕이고, 스윕이 있는 이유가 이것입니다.)

**그런데도 좁은 쪽에서 시작합니다 — 이유는 정밀도가 아니라 공정입니다.**
**넓히는 것은 되지만 좁히는 것은 안 됩니다.** 그래서 기본값을 번짐 최소 근처인
**0.30** 에 두고 필요하면 칼로 넓힙니다. 0.4 노즐이 0.30 을 못 갈라 주고 붙어
나오는 것이 보통인데, **그것이 의도한 경로**입니다.

⚠ **하한 0.16.** 슬릿이 판 두께(0.8)에 비해 좁으면 **터널**이 되어 부채꼴 가장자리가
깎입니다 — 비네팅 = `PLATE_T·tan(부채꼴/2)/SLIT_W`, 0.16 에서 20 % 입니다.
그 아래로는 깎지 마십시오. assert 가 막습니다.

> **게이지는 커터칼 날이 종이보다 낫습니다.** 강철이라 두께가 ±0.02 로 일정하고
> (복사용지는 ±10 %), **자를 때 쓴 그 날을 그대로 밀어 넣으면** 슬릿이 정확히 날
> 두께가 됩니다 — 자기 게이지입니다. 커터칼 날이 슬릿 턱에서 물러난 자리로
> **자르는 도구 겸 게이지**로 돌아온 셈입니다.
> 억지로 밀지 말고 **미끄러져 들어가는지**로 판정하십시오.

> **★ 폭이 얼마나 일정해야 하나 — 거의 상관없습니다.** 0.27~0.38 로 들쑥날쑥해도
> 무해합니다. **y 방향(슬릿 길이 쪽)으로는 광학 파워가 없어서**(프리즘이 축이 y 인
> 반원통 = y 로는 평판, 조리개 개구 6 mm) 칩의 한 점이 슬릿 길이 **전체를 적분해서**
> 봅니다 — 폭 변동은 해상되지 않고 **평균**됩니다.
> 유효 개구 = √(평균² + 편차²) = 0.325 → 0.327 (**+0.5 %**), 번짐 8.01 → 8.03 px.
> 그 결과는 **고정 커널**이고 프레넬 곡선은 강체 평행이동하므로, 컨볼루션은 위치를
> 상수만큼 옮길 뿐 — **2점 보정이 흡수**합니다. 유효 중심이 밀리는 최대량도
> 0.055 mm(편심 예산의 15 %)이고 역시 고정값입니다.
>
> 오히려 **판이 홈 안에서 도는 쪽이 큽니다** — 여유 0.15 · 판 길이 9.5 → 최대 0.90°,
> 슬릿 길이 8 에서 유효 폭 **+0.126 mm**(0.30 → 0.326, +8.5 %). 그래도 무해합니다.
>
> ⚠ **지켜야 하는 것은 균일함이 아니라 넷입니다** — 움직이지 않을 것(1.26 µm =
> 예산 전체) · 국소적으로 **0.16 아래로 내려가지 않을 것**(터널) · 길이 8 이 끊기지
> 않을 것 · **불투명할 것**.
>
> **게이지는 9 mm 커터칼 날로 확정합니다** (0.38~0.45). 18 mm(0.50)보다 최적에
> 가깝고, 최종 슬릿 폭이 곧 그 날 두께가 되므로 **가진 날을 실측해 적어 두십시오.**

> ①~④ 는 **"따로 정한 두 치수를 대조한 적이 없다"**, ⑤ 는 **"고칠 때 한 줄이 남았다"** 입니다
> (포켓 반지름 vs 살 높이 · 분할면 위치 vs 창 폭 · 블록 두께 vs 창 사면 ·
> 광창 윗면 vs 확산판 슬롯 아랫면).
> 이제 `scad/optical_bench.scad` 에 assert 열네 개가 있고, 검사가 여섯 개입니다.

### 2026-09-04 — 슬릿을 커터칼 날에서 **인쇄한 판**으로 바꿨다 (설계 변경)

옛 설계는 커터칼 날 두 장을 마주 보게 끼우고 복사용지 심으로 폭을 맞추는 것이었습니다.
확인해 보니 셋이 한꺼번에 걸립니다.

**(1) 실물 날이 안 들어갑니다.** 날 슬롯이 받아 주는 것은 한 장당 **4.76 × 7 × 0.5**
인데(플러그 9.7 을 양쪽에서 나눠 쓰고, 플러그는 캐비티에서 편측 0.15 밖에 안 논다),
9 mm 커터칼 날은 등까지 폭이 9, 18 mm 날은 18, 면도날은 22 입니다.
게다가 스냅오프 날은 **45° 로 쪼개지므로 한 마디가 평행사변형**이라 담아야 할 것은
날끝 길이가 아니라 **감싸는 사각형**입니다.

| | 마디 날끝 길이 | 깊이 | 감싸는 사각형 | 필요한 `HUB_Y` |
|---|---|---|---|---|
| 9 mm 날 한 마디 | **6.15** | 9 | 15.2 × 9 | 25.3 |
| 9 mm 날 두 마디 | 12.31 | 9 | 21.3 × 9 | **31.4** |
| 18 mm 날 한 마디 | 12.50 | 18 | 30.5 × 18 | 훨씬 더 |

지금 `HUB_Y` 가 20.8 입니다. **그리고 금속 날은 네모나게 가공할 수 없습니다** —
생긴 그대로 담아야 합니다.

**(2) 한 마디로는 빔을 못 덮습니다.** 날끝이 6.15 mm 인데 빔 폭 `CH_Y` 가 7 입니다.

**(3) 심을 빼고 나면 날을 붙들어 두는 것이 없습니다.** 그리고 이게 제일 큽니다 —
슬릿이 옆으로 **1.26 µm** 움직이면 예산 전체(0.066 PSU)를 먹습니다.

> **편심의 산수** — 슬릿에서 측정점 M 까지가 **70 mm**(유리 30 + 공기 40)입니다.
> 슬릿이 d 만큼 밀리면 M 에서의 입사각이 d/70 rad 바뀌고, 프레넬 곡선이 **통째로
> 평행이동**합니다(폭이 변하는 것이 아닙니다). 지렛대가 90 이므로 칩에서는 1.29 d,
> 칩 1 µm 가 0.0408 PSU 이니 **슬릿 1 µm = 0.052 PSU** 입니다.
>
> | | |
> |---|---|
> | 정적 편심 (가만히 치우쳐 있음) | **무해.** 통째 평행이동이라 2점 보정이 상수로 흡수한다.<br>한계는 정밀도가 아니라 **부채꼴 여유 ±0.30° = 편심 ±0.37 mm** |
> | 동적 편심 (보정 후 움직임) | **1.26 µm 면 예산 전체를 먹는다.** 이것이 진짜 요구다 |
>
> 그리고 **심은 폭만 정하고 위치는 아무것도 정하지 않습니다.**

**그래서 슬릿을 인쇄합니다.** 인쇄한 판은 (3)을 **원천적으로** 없앱니다 — 한 몸이라
움직일 방법이 없습니다. (1)(2)도 사라집니다. 이 부품에서 중요한 것은 정밀도가 아니라
**재현성**이므로 이쪽이 옳습니다.

**왜 인쇄로 되는가 — 직선도 요구가 직관보다 훨씬 느슨합니다.**
`src/profile_crb.py` 의 결론이 **"번짐을 100배 늘려도 σ 는 2.5배만 나빠진다"** 였습니다.
가장자리의 물결(압출 폭 변동, ±0.05 급)은 **번짐으로만** 나타나고, 가장자리의 평균
위치는 8 mm 를 따라 평균되어 오히려 안정적입니다. 남는 편심은 고정값이라 위 표대로
보정이 흡수합니다.

**왜 바닥면인가** — 옆벽은 레이어 줄무늬가 남지만 **바닥면은 베드가 찍어 주므로
평평합니다.** 판을 눕혀서 인쇄하면 슬릿을 정의하는 면이 바닥면이 됩니다.

**왜 따로 뽑는가 — 한 몸이면 다듬을 수 없습니다.** 카드로 빼 놓으면
(가) 불빛에 비춰 눈으로 검사하고, (나) 강철자를 대고 칼로 다듬고,
(다) **폭이 다른 판을 여러 장 뽑아 갈아 끼웁니다** — §6 의 슬릿 폭 스윕이 바로
이것이고, 본체도 플러그도 다시 안 뽑습니다.

**형상** — `slit_plate` 6.7 × 9.5 × 0.8, 슬릿 `SLIT_W` × 8.
플러그 안쪽 끝의 홈에 **y 로 떨어뜨려 넣습니다.** 홈이 x 로 막혀 있어 좌우 위치가
정해지고(편심 ±0.15), 앞으로 빠지지 않는 이유는 블록 캐비티(10 × 10)와 광로
채널(5 × 7) 사이의 **턱**이 판을 막기 때문입니다 — 잡아 주는 부품이 따로 없습니다.
슬릿을 8 로 (빔 7 보다 1 길게) 잡아 판이 y 로 ±0.5 놀아도 빔을 덮습니다.

**슬릿 폭은 0.19 를 맞출 필요가 없습니다.** 0.19 는 √(λ·검출팔) 의 최적점일 뿐이고
위 CRB 결론상 그보다 넓어도 σ 는 거의 안 나빠집니다. **인쇄로 나온 값을 실측해
적어 두는 것**이 규약입니다.

- `SLIT_W = 0.45` — 0.4 노즐이 갈라 주는 최소에 가깝습니다.
- `SLIT_W = 0` — **막힌 카드로 뽑아 후가공으로 갈라냅니다.** 바닥면에 강철자를 대고
  새 칼날로 한 번에 긋습니다. 커프가 0.05~0.1 이라 더 좁은 슬릿을 얻고, 직선도는
  강철자가 줍니다. 여러 번 그으면 넓어집니다.

새 검사 : `PART="platecheck"`(판이 홈에 들어가는가) · `PART="plugcheck"`(판을 끼운
플러그가 캐비티에 들어가는가) · `PART="raycheck"`(광로가 뚫렸는가).
raycheck 는 `-D SLIT_W=0` 으로 돌리면 실제로 **막힌 것을 잡아냅니다** — 검사가
동작한다는 것까지 확인했습니다.

### 2026-09-04 — 실링 방침 (설계 변경)

**덮는 것은 막는 것이 아닙니다.** 옛 설계는 창을 프리즘보다 좁게(반폭 4) 만들어
편측 1 mm 의 살이 포켓 틈을 "덮게" 했습니다. ①로 그 살은 애초에 닿지도 않았고,
닿았더라도 살 밑 0.05 mm 틈은 모세관압이 **+1 kPa**(2γcosθ/h, PETG θ≈70°)라 액을
**빨아들입니다**. 빨린 액은 닦이지도 마르지도 않아 표준액↔시료 **이월**이 됩니다 —
20 µL 중 2 µL 만 이월돼도 35 와 34 사이에서 0.1 PSU 급이고, 예산 전체(0.066)보다
큽니다. 광학·기구 어떤 개선보다 큽니다.

방침을 바꿨습니다 — **면끼리 맞물리게 하지 않는다.** FDM 이 지킬 수 없는 약속입니다.
대신 액이 닿는 면을 **유리와 굳은 탄성체로만** 만듭니다.

| 자리 | 처리 | 상수 |
|---|---|---|
| 프리즘 옆 틈(편측 0.35) | 위 2 mm 를 0.8 로 넓힌 **실런트 채널**. 굳으면 창의 y 벽이 된다 | `SEAL_W` `SEAL_D` |
| 살 아랫면 | 창 둘레 |x|≤12 를 0.4 파낸 **실런트 링**. 프리즘 상면까지 실제 0.55 | `SEAL_REC` `SEAL_RX` |
| 분할면 크랙 | 블록 윗면에 열린 **주입 슬롯** 2 곳. 위 둘까지 흘러 한 번에 찬다 | `SEAM_X` `SEAM_SX` `SEAM_W` |

창은 프리즘 **두께 전폭**까지 엽니다(반폭 5.8). 빔 여유가 편측 0.5 → 1.5 mm 로
넓어지는 것은 덤입니다. 액적 깊이는 20 µL /(14 × 11.6) = **0.12 mm** 라 `SEAL_REC`
0.4 안에 들어옵니다 — 즉 액은 인쇄물 벽에 닿지 않습니다(assert 로 지킵니다).

**실런트는 MS 폴리머(변성실란)** 입니다. 금기 :

| | 왜 안 되는가 |
|---|---|
| 순간접착제(CA) | PMMA 크레이징 + 증기가 광학면에 백화로 앉는다 |
| 아세톡시(수조용) 실리콘 | 아세트산이 응력 있는 PMMA 를 크레이징, **베어 칩** 패드·본딩와이어 부식 |
| 에폭시 | 강성이라 프리즘에 응력(§3 금기), 분해 불가. 프리즘 실측 전이라 되돌릴 수 있어야 한다 |
| UV 경화 | 2단계 UVC 에 열화 (기존 금기) |
| 중성경화 실리콘 | 아세트산은 없지만 **저분자 실록산이 광학면으로 이행**해 발수화시킨다. 20 µL 이 패치 8.6 mm 를 덮으려면 접촉각이 20° 급이어야 해서 치명적이다 |
| 폴리우레탄 | 접착이 너무 강해 프리즘 분해 불가, 황변 |

시공 순서 — ① 광학면을 IPA 로 닦고 **마스킹**, ② 프리즘을 B 반쪽 포켓에 얹고 A 를
덮어 조인다, ③ 윗면 주입 슬롯 두 곳에 주사기로 넣어 링·채널까지 채운다, ④ 창 안으로
넘친 것은 굳기 전에 면봉으로 닦는다, ⑤ **완전 경화(24~48 h) 뒤에** 칩을 얹는다 —
경화 증기 속에 베어 칩을 두지 마십시오.

### 2026-09-04 — 차광 슈라우드를 뚜껑으로 바꿨다 (설계 변경)

상부 전체를 덮는 204 × 42 × 57 슈라우드를 버리고, 허브 A·B 의 **귀에 M3 로 달린
작은 판**으로 바꿨습니다. 근거는 **차광의 본체가 사실 창을 막는 보스**라는 것입니다.
슈라우드가 추가로 하던 일은 배선 슬롯 두 곳의 차광뿐이었고, 그건 슬롯을 y 로 4.5 mm
꺾어(`SLOT_OFF`) 끝면에서 칩·LED 캐비티가 직선으로 안 보이게 하는 편이 훨씬 쌉니다.
마감은 배선 후 **검정 실런트로 끝면을 메우는 것**입니다. 남는 산란광은 다크프레임이
뺍니다(§6-1).

⚠ **대신 하중 경로가 생겼습니다.** 슈라우드는 벽이 바닥판에 내려앉아 본체를 안
건드렸습니다. 그래서 셋을 지킵니다.

1. **래치·자석·스냅을 달지 않는다.** 닫는 힘은 자중(약 0.2 N)뿐입니다. 손으로 누르면
   검출팔 60 mm 끝의 칩이 미끄러집니다 (1 µm = 0.061 PSU).
2. **피벗 나사를 조이지 않는다.** 구멍이 3.8 에 M3 라 0.4 유격이 있고, 그 유격이
   보스가 창 사면에서 스스로 중심을 잡게 합니다. 뻑뻑하면 그걸 방해하고 옆으로 밉니다.
3. 판을 왼쪽으로 길게 빼 무게중심이 보스보다 멀게 합니다(자중의 약 1.35 배).

⚠ **자중이 슈라우드(약 0.7 N)보다 작으므로 증기 웰의 밀폐가 사면 인쇄 품질에 더
의존합니다.** 1순위 검증에 **"뚜껑 닫고 10 분 방치 후 재측정"** 을 넣으십시오.
새면 대안은 보스 사면에 실런트로 성형하는 탄성 립입니다(부품이 늘지 않습니다).

새 검사 : `PART="swingcheck"` (5°~90° 를 겹쳐 여는 동안의 간섭. 비어야 정상).

### 이전 (2026-08-31 ~ 09-02)

**두 광학팔 사이각 계산 오류** (2026-08-31 수정 완료)

`design_calc.py`에서 `180 − 2θc = 56.14°`로 계산했으나 실제로는 **`2θc = 123.86°`** 입니다.
θc = 45°일 때 두 식이 우연히 일치해서 검증에서 놓쳤습니다. 이대로 가공했으면 광로가 전혀 맞지 않았을 것입니다.

수정본에는 가공에 쓰기 편한 **"각 팔의 수평 기준 각도 = 28.07°"** 항목도 추가했습니다.

> **이 123.86°는 BK7(n=1.5168) 기준입니다.** 프리즘을 아크릴(n=1.491)로 바꾸면 **127.70°**,
> 활꼴 60×20이면 **149.26°** 가 됩니다. 형상·재질별 값은 `src/segment_optics.py`로 확인하십시오.

> **주의**: `fig_optics.png`(설계 문서 3장)에는 이 각도 표기가 없어 영향이 없지만, 혹시 초기 버전 그림을 따로 저장해 두셨다면 "두 팔 사이각 56.1°"라고 적힌 것은 폐기하십시오.

**AR 코팅 판단 오류** (2026-09-01 수정 완료)

"반사방지 코팅이 있으면 임계각 측정이 안 된다"고 설명했으나 **틀렸습니다.**
접선 방향 파수 n·sinθ가 모든 층을 통해 보존되므로 임계각은 **첫 매질과 마지막 매질만으로** 결정됩니다.
전달행렬법으로 확인한 결과 맨유리·AR 107 nm·AR 200 nm·저굴절 1.30 코팅 모두 임계각이 동일했습니다.
오히려 AR 코팅은 임계각 아래 반사율을 낮춰 경계 대비를 개선합니다.
→ **AR 코팅 유무는 구매 판단 기준이 아닙니다.** 재현: `src/segment_optics.py`

**아크릴 해수 내구성 판단 오류** (2026-09-01 수정 완료)

"수조물에 아크릴이 변질된다"고 적었으나 **틀렸습니다.** PMMA는 해수에서 화학적으로 안정합니다
(아크릴 수조가 표준 재질). 남은 유효한 우려는 UVC 분해, 용제(IPA·아세톤) 크레이징, 긁힘 세 가지뿐입니다.

**회절 모델 오류 — 좁은 슬릿이 오히려 불리** (2026-09-01 2차 수정 완료)

경계 폭을 나이프에지 프레넬 스케일 `√(λL)`로 계산했으나, 그 식은 **넓은 개구**에 맞는 것입니다.
설계는 0.2 mm 슬릿 + 250 mm 자유공간이고 `L ≫ w²/λ = 68 mm` 이므로 **프라운호퍼 영역**이라
실제 회절 폭은 `λL/w` 입니다.

| | 옛 계산 `√(λL)` | 실제 `λL/w` |
|---|---|---|
| 회절 폭 @250 mm, 슬릿 0.2 mm | 6.0 px | **11.6 px** |
| 경계 폭 합계 (활꼴 60×20) | 6.6 PSU | **10.8 PSU** |

두 식은 `w = √(λL) = 0.384 mm` 에서 정확히 만나며, 그 지점이 무렌즈 배치의 최적 슬릿입니다.
**슬릿을 좁힐수록 좋아진다는 직관이 틀렸습니다** — 0.05 mm 로 좁히면 39 PSU 로 폭발합니다.

파생 결론 두 가지가 뒤집혔습니다.

- **검출팔 길이는 각도 분해능을 개선하지 않습니다.** 프라운호퍼 영역에서 회절 폭은 `L`에
  **비례**하고 스팬도 `L`에 비례하므로 PSU 환산 경계 폭이 팔 길이에 무관합니다
  (60 mm 11.1 PSU ↔ 730 mm 11.2 PSU). 옛 계산의 "팔을 730 mm 로 늘리면 6.6 → 5.7 로 좋아진다"는
  틀렸습니다. 팔이 정하는 것은 px/PSU 배율, 즉 픽셀 샘플링뿐입니다.
  **250 mm 유지라는 결론 자체는 그대로**이되 근거가 바뀌었습니다.
- 최적 슬릿은 0.3 mm 부근이고 현 설계 0.2 mm 는 16% 손해입니다. 바닥이 완만해 큰 결정은 아닙니다.

재현: `python src/segment_optics.py` 의 `[검출팔 길이]`·`[슬릿 폭 최적화]` 절.

**오차예산의 온도 항목이 잘못 붙어 있었음** (2026-09-01 2차 수정 완료)

`design_calc.py` 예산표에 `('잔류 온도차 (2 degC 가정)', diff * 2)` 로 적혀 있었습니다.
`diff`(0.0015 PSU/°C)는 **둘이 같은 온도로 함께 드리프트할 때**의 공통모드 잔차인데,
라벨은 **둘 사이의 온도차**를 말하고 있었습니다. 후자에 곱할 계수는 0.667 PSU/°C 이고,
라벨대로 2 °C 차이라면 0.003 PSU 가 아니라 **1.33 PSU** 입니다.

즉 예산표에서 가장 위험한 항목이 사실상 0으로 위장돼 있었습니다. 고친 내용:

- 라벨을 `공통모드 온도 드리프트 (2 degC)` 로 바로잡음
- `시료-표준액 온도차 잔차` 항목을 새로 추가 (0.667 × 센서 분해능 0.0625 = **0.042 PSU**)
- RSS 합계 0.117 → **0.124 PSU**. 목표 ±0.12 는 사실상 그대로지만 근거가 정직해졌습니다

**파장 민감도 과대평가 — 프리즘 분산 누락** (2026-09-01 2차 수정 완료)

`psu_per_nm = |dn_water/dλ| / (dn/dS)` 로 계산해 **0.19 PSU/nm** 을 얻었으나,
임계각은 `θc = asin(n_s / n_p)` 이므로 프리즘의 분산도 들어가야 합니다.

```
d(θc)/dλ = [ dn_s/dλ − sin(θc)·dn_p/dλ ] / (n_p·cos θc)
```

물 `−3.41×10⁻⁵/nm` 과 유리·아크릴 `−4.1×10⁻⁵/nm` 이 같은 부호로 비슷한 크기라
대괄호 안이 거의 상쇄됩니다. 실제 값은 **0.013 PSU/nm (BK7) / 0.018 (아크릴)** 로 **14배 작습니다.**

백색 LED 번짐도 28 PSU 가 아니라 2.0 PSU 입니다 (여전히 사용 불가라는 결론은 유지).

**프리즘 포켓 곡률 중심 부호 오류** (2026-09-02 수정 완료)

`scad/optical_bench.scad` 의 `P_CZ = P_SAG - P_R` 가 곡률 중심을 측정면 **아래**에 두고 있었습니다.
포켓이 입구 60 mm → 깊이 20 mm 지점 63 mm 로 **벌어지는 언더컷**이 되어 프리즘이 들어가지도,
인쇄되지도 않는 형상이었습니다. 파일 머리말대로 OpenSCAD 렌더 검증을 못 해서 남아 있던 오류입니다.

곡률 중심은 측정면 **위**(시료 쪽)에 있습니다 — `P_CZ = P_R - P_SAG`. 정확한 반원이면 `P_CZ = 0`,
즉 **곡률 중심이 곧 측정점**이고 이때 주광선이 곡면을 0° 로 통과합니다(굴절 없음).
→ 형상과 파생 상수는 [`docs/head.html`](docs/head.html) 이 실시간으로 다시 풉니다.

**접착 패드가 허브 안에 묻혀 있던 문제** (2026-09-02 수정 완료)

패드를 곡면 통과점에서 46 mm 밖에 두면 DR-257(현 150) 기준 허브가 **320 mm** 로 커져
취미 프린터 베드를 넘습니다. 게다가 블록 폭이 패드보다 커지면 광로가 허브 밖으로 못 나갑니다.
→ **블록은 프리즘만 감싸고(현 + 벽), 패드는 팔 축을 따라 돌출하는 보스**로 바꿨습니다.
DR-257 기준 블록 162 / 보스 포함 203 × 90 mm 로 220 베드에 그대로 들어갑니다.
M3 4본도 포켓과 광로를 모두 피한 자리(옆구리 낮은 곳 2 + 정점 아래 2)로 옮겼습니다.
클램셸 정렬 페그도 `rotate` 뒤에 `translate` 를 해서 축이 Y 로 새 나가 있던 것을 고쳤습니다.

**"경계 폭" 모델 자체의 한계** (2026-09-02 발견)

이 문서와 `segment_optics.py` 는 프로파일을 **폭 W 의 계단**으로 보고
`σ ≈ W/(SNR·√N)` 를 씁니다. 그런데 실제 프로파일은 계단이 아니라 **프레넬 곡선**이고,
염도가 바뀌면 그 곡선이 통째로 평행이동합니다. 알려진 모양의 이동량을 추정하는 한계는
폭이 아니라 **기울기의 제곱합**이 정합니다 (Cramér-Rao):

```
sigma_S = sigma_I / sqrt( sum_i (dI_i/dS)^2 )
```

`src/profile_crb.py` 로 계산한 결과 (SNR 1300 = 데이터시트 잡음 1 mV rms + 10비트 ADC):

| 번짐 [PSU] | σ (1프레임) | | 팔 [mm] | PSU/px | σ (1프레임) |
|---|---|---|---|---|---|
| 0.72 (렌즈) | 0.0059 | | 160 | 1.44 | 0.0080 |
| 8.94 (확정) | 0.0091 | | 100 | 2.31 | 0.0091 |
| 20 | 0.0111 | | 60 | 3.85 | 0.0114 |
| 80 | 0.0148 | | 30 | 7.76 | 0.0142 |

**세 가지가 따라옵니다.**

1. **잡음 한계는 목표보다 10배 이상 여유롭습니다** (0.009 PSU vs 0.12 PSU).
   즉 **오차예산 전체가 계통오차 몫**입니다 — 미광(§`ILLUMINATION` 5), 다크·플랫필드(§6),
   기계·열 드리프트, 스펙트럼. 이쪽에 노력을 쓰는 것이 맞습니다.
2. **번짐과 팔 길이가 정밀도를 거의 안 정합니다.** 번짐을 0.72 → 80 PSU 로 100배 늘려도
   σ 는 2.5배만 나빠집니다. 프레넬 곡선의 기울기가 지배하기 때문입니다.
   → **푸리에 렌즈의 이득이 "경계 12배 날카로움"이 아니라 σ 1.5배**입니다.
   렌즈의 진짜 값은 날카로움이 아니라 **조명팔을 없앨 수 있다는 것**입니다.
3. **짧은 팔이 다시 가능해집니다.** 앞서 "30~40 mm 는 안 됨"이라고 한 것은
   **계단 모델 + 센트로이드** 전제였습니다. 프로파일을 모델 피팅하면 30 mm 에서도
   잡음 한계가 0.014 PSU 입니다. 단 1 화소 = 7.76 PSU 라 **화소 단위 계통오차의 레버리지가
   커지므로**(PRNU·먼지·불량화소) 플랫필드가 필수가 되고, 여유가 줄어듭니다.

> **펌웨어 요구가 하나 생깁니다.** 이 이득은 **센트로이드나 문턱값이 아니라 프레넬 모델
> 피팅**으로만 얻어집니다. 2순위 아두이노 스케치에 그 사실을 반영해야 합니다.

**투과형(핸드 굴절계)과 반사형은 정보량이 정확히 같습니다** (2026-09-02)

핸드 굴절계는 **상부로 빛을 넣고 손잡이로 눈으로 보는 투과형**입니다 — 시료를 통과한 빛이
프리즘 안에서 θc 이하만 채우고 그 차단선이 경계입니다. psu-meter 는 **반사형**입니다.

`T = 1 − R` 이므로 `dT/dS = −dR/dS`, 즉 **두 구조의 CRB 가 소수점까지 동일합니다**
(위 표의 두 열). 따라서 선택은 정밀도가 아니라 **기계·환경**으로 해야 합니다.

| | 투과형 (핸드 굴절계) | 반사형 (psu-meter 현재) |
|---|---|---|
| 광원 위치 | 시료 **위** — 조명팔 불필요 | 프리즘 **안** — 조명팔 또는 푸리에 렌즈 필요 |
| 시료 탁도·색 | 빛이 시료를 **통과**하므로 영향 받음 | **면만 봄** (θc 위는 침투깊이 ~0.1 µm) — **면역** |
| 차광 | 상부가 광원이라 못 막음 | 막아야 하고, 막을 수 있음 |
| 상용 정밀 기기 | 저가 핸드헬드 | **Vaisala PR-33 등 공정용** |

> **§5-6 의 "실내등 불가" 결론은 반사형 전제였습니다.** 투과형으로 가면 상부 입광이
> 오염이 아니라 신호입니다. 다만 광원은 여전히 **불투명 캡 안의 590 nm LED** 여야 합니다 —
> 실내등은 스펙트럼이 불안정하고(백색이면 번짐 2.0 PSU, 게다가 등 종류·시각에 따라 변함)
> 방의 각도별 휘도 분포가 드리프트합니다. 캡이 **차광과 광원 홀더를 겸하는 것**이
> 핸드 굴절계의 구조이고, 그게 합리적입니다.

**표면 평면도 요구 과장** (2026-09-01 수정 완료)

조명 폭 10 mm를 가정해 사출 아크릴이 8.6~22.9 PSU 번진다고 했으나, 설계 슬릿은 처음부터 0.2 mm입니다.
슬릿 폭별 번짐은 10 mm → 22.85 / 3 mm → 2.49 / 1 mm → 0.28 / **0.2 mm → 0.01 PSU** 입니다.
좁은 슬릿 아래에서 완만한 곡률은 번짐이 아니라 **상수 오프셋**이 되고 2점 보정이 그것을 지웁니다.
→ **광학 등급 평면도(λ/4 등)는 필요 없습니다.**

---

## 5. 검증된 것과 안 된 것

### 계산으로 확정 (`design_calc.py`로 재현 가능)

굴절률 경험식은 [Oregon State University, Parrish Research Group](https://research.engr.oregonstate.edu/parrish/index-refraction-seawater-and-freshwater-function-wavelength-and-temperature)의 계수를 사용했습니다. 본 프로젝트의 **모든 수치는 이 식에서 직접 계산**한 것이며 인용값이 아닙니다.

- dn/dS = 1.81×10⁻⁴ /PSU, dn/dT = −1.21×10⁻⁴ /°C, dn/dλ(물) = −3.41×10⁻⁵ /nm
- 임계각 61.93° (BK7, 34 PSU, 26 °C, 590 nm), 감도 253 µrad/PSU
- 250 mm에서 0.997 px/PSU, RO-DI 보정점은 33.6 px 떨어짐
- 파장 민감도 0.013 PSU/nm (프리즘 분산 포함, §4)
- 오차 예산 RSS = **0.124 PSU** (표준액 정확도 0.1 PSU가 지배적, 다음이 온도차 잔차 0.042)

### 2026-09-02 확정 — 검출기

**TSL1401CL 베어 칩**(CL 패키지, 8패드 LGA 9.4×3.0×1.2 mm). 모듈이 아닙니다.
데이터시트를 저장소에 넣었습니다 — [`docs/datasheets/TSL1401CL.pdf`](docs/datasheets/TSL1401CL.pdf).
사양·핀배열·배선 요령은 [`BOM.md`](BOM.md) 1-1.

- **렌즈 위험 소멸** — 뗄 렌즈가 없습니다
- **패키지 기하가 데이터시트에 있어 실측이 불필요** — 어레이 8.064 mm, 화소 피치 63.5 µm,
  어레이 중심선은 패키지 중심선에서 폭 방향 0.305 mm 치우침
- **어레이 8.064 mm 가 부채꼴 상한을 만듭니다** — 2° 는 8.73 mm 로 넘칩니다
  ([`ILLUMINATION.md`](ILLUMINATION.md) §2 "부채꼴 상한")
- **PRNU ±4 % 는 다크 차감으로 안 지워집니다** — 플랫필드 필요
  ([`ILLUMINATION.md`](ILLUMINATION.md) §6-4)
- 손배선 가능 — 패드 피치 2.5 mm, 필요한 선 5가닥. **0.1 µF 를 칩 위에 직접** 붙일 것

### 확인된 조달처

- TSL1401CL 모듈 — 알리익스프레스 다수 (한 곳 표기가 약 US$58, 편차 큼)
- 연동펌프 — 디바이스마트 [BWP-2752P](https://www.devicemart.co.kr/goods/view?no=12759593), [ada-3910](https://www.devicemart.co.kr/goods/view?no=15531016)

### 확인 못 한 것 (⚠ 인수받는 사람이 확인해야 함)

| 항목 | 사유 |
|---|---|
| **디바이스마트 가격·재고 전반** | 사이트가 자동 접근을 차단. 검색엔진에 색인된 URL 2건 외에는 페이지를 열지 못함 |
| ~~**BK7 반원통 렌즈 실제 구매 가능성**~~ | **2026-09-01 해결.** 국내 아크릴 교구로 확정. [`PRISM_SOURCING.md`](PRISM_SOURCING.md) 참조 |
| ~~**TSL1401 모듈의 렌즈 탈거 가능 여부**~~ | **2026-09-02 해결 — 항목 자체가 없어짐.** **베어 칩(TSL1401CL CL 패키지)** 으로 확정해서 뗄 렌즈가 아예 없습니다. (참고로 모듈 CJMCU-1401 도 렌즈가 나사식이라 탈거는 가능했습니다.) 데이터시트를 저장소에 넣었습니다 — [`docs/datasheets/TSL1401CL.pdf`](docs/datasheets/TSL1401CL.pdf) |
| ~~**590 nm LED의 실제 반치폭(FWHM)**~~ | **2026-09-01 2차 해결.** 프리즘 분산 상쇄로 40 nm 라도 0.52 PSU. 발주 조건 아님 (§4) |
| **일체형 모듈의 가격** | 제원은 2026-09-01 확인 완료 (§3 "상용 모듈을 쓰지 않는 이유") — Vaisala PR-33-AC 재현성 ±0.55 PSU, Pyxis RT-50 ±1.65 PSU 로 **둘 다 목표 미달이고 상시 침지형**. 다만 **가격은 견적 문의형이라 여전히 미확인** |

### 전혀 검증 안 된 것

**실물 제작·측정이 하나도 없습니다.** 전부 계산상 설계입니다. 특히 다음은 실물에서 깨질 수 있습니다.

- 그림자 경계가 실제로 관측 가능한 대비로 생기는지
- 미광 차단이 충분한지 — **요구사항·판정 기준·다크 프레임 규약은 2026-09-02 정리됨**
  ([`ILLUMINATION.md`](ILLUMINATION.md) §5·§6). 상자보다 **시료 쪽 차광**이 중요하다는
  것이 이때 나왔습니다
- 기포 대책이 실효가 있는지
- 3D 출력물의 방수·강성이 버티는지
- **서브픽셀 1/20 px 가정이 현실적인지** (2026-09-01 추가 — 아래 참조).
  계산상으로는 SNR 100 + 16프레임이면 충족되므로, 실물에서 볼 것은 폭이 아니라 **프로파일 재현성**입니다.

### ⚠ 오차예산에 없던 항목 — 회절 (2026-09-01 발견, 09-01 2차 정정)

> 아래는 §4의 "회절 모델 오류"를 반영한 최종본입니다.

`src/segment_optics.py`로 검출팔 길이를 스캔하다 발견했습니다.
슬릿 0.2 mm · 팔 250 mm 에서 회절 폭은 **0.737 mm = 11.6 px**,
기하 번짐(활꼴 5.2 px)과 스펙트럼(0.4 px)까지 합치면 **경계 폭 12.7 px = 10.8 PSU** 입니다.
즉 그림자 경계는 기하학적 번짐보다 **회절에 의해 훨씬 넓게 퍼집니다.**

**팔 길이로는 못 고칩니다** (회절 폭과 스팬이 둘 다 L에 비례). **슬릿 폭으로도 거의 못 고칩니다**
(최적 0.3 mm 에서 9.4 PSU, 16% 개선이 전부). 각도 분해능이 `λ/w`로 묶여 있기 때문입니다.

> **2026-09-02 — 바로 그 이유로 검출팔을 250 → 160 mm 로 줄였습니다.**
> 회절 폭과 스팬이 함께 L 에 비례한다는 것은 **경계 폭을 PSU 로 환산하면 팔 길이에
> 무관하다**는 뜻이기도 합니다(반원 60×30 에서 10.8~11.3 PSU 로 평평). 즉 팔을 줄여도
> 잡음 한계 정밀도가 나빠지지 않습니다. 전체 스팬 503 → 341 mm, 바닥판 570 → 410 mm.
> 짧아지면서 나빠지는 것은 에지 알고리즘 계통 바이어스의 PSU 환산뿐이고,
> **최종 확정은 일자 100 mm** 입니다 — 전체 스팬 233 mm, 바닥판 278 × 90 mm,
> 측정면 높이 74 mm. 0.05 px 바이어스가 0.116 PSU 로 목표 ±0.12 PSU 안입니다.
> 슬릿 **0.3 mm** 와 부채꼴 **4.0°** 를 같이 확정했습니다 — 100 mm 에서 슬릿 0.3 이
> 경계 폭 최소(3.84 px = 8.94 PSU)이고, 부채꼴 4.0° 가 어레이 8.064 mm 의 87 %를 씁니다
> (2° 면 43 %만, 5° 면 넘칩니다).
>
> **거울로 접는 안(광로 160 mm, 상자 170 × 97 mm)은 검토했으나 미채택** — 성능은
> 유지되지만 전면반사 거울 4장이 필요하고(후면 유리 거울은 4 % 유령상이 19 px 어긋나
> 가짜 경계를 만듭니다) 미광·정렬이 같이 어려워집니다. 기록은
> [`ILLUMINATION.md`](ILLUMINATION.md) §2 "팔 길이".
>
> **팔은 착탈식입니다.** 허브의 중공 스피곳(12 × 15 × 8)에 덕트 칼라를 끼워 M3 2본.
> 각도는 허브에 인쇄된 기준면이 잡으므로 떼어도 안 변하고, 애초에 2점 보정이 매 사이클
> 기울기까지 다시 재므로 사이클 사이의 변화는 전부 흡수됩니다. 한 사이클(몇 분) 안에서의
> 움직임만 문제입니다. **받침대 일체** — 총 266 g, 무게중심 21 mm, 넘어짐 기울임 65°,
> 넘어뜨리는 힘 5.6 N (튜브 반력 대비 19배 여유). M4 수평조절 3점.

#### 그런데 이것이 설계를 깨지는 않습니다

경계가 넓다는 것 자체는 오차가 아닙니다. **매 사이클 2점 보정이 같은 모양의 경계를 세 번
(RO-DI · 표준액 · 시료) 재고 그 이동량만 쓰므로, 모양이 재현되기만 하면 상쇄됩니다.**
넓은 경계가 실제로 요구하는 것은 SNR 입니다.

```
센트로이드 정밀도  σ ≈ W / (SNR·√N)      W = 경계 폭 12.7 px, N = 평균 프레임 수
목표 0.05 px  →  SNR·√N ≥ 255
                 SNR 100 + 16프레임 평균 = 400   (충족)
```

10비트 ADC 와 제대로 된 차광이면 어려운 수준이 아닙니다.
**따라서 진짜 위험은 경계의 폭이 아니라 경계 모양의 재현성입니다** —
미광, 정렬 드리프트, 프레넬 링잉이 프로파일 모양을 바꾸면 2점 보정이 그것을 못 지웁니다.

**1순위 검증에서 경계 프로파일을 프레임 단위로 저장해 폭과 재현성을 실측하십시오.**

#### 근본적으로 해결하려면 — 렌즈 1장 (선택)

검출기를 볼록렌즈의 뒤초점면에 놓으면(푸리에면 배치, 상용 굴절계의 구조) 위치가 곧 각도가 되어
**조명 스폿 크기와 각도 분해능이 분리됩니다.** 개구를 넓힐 수 있으므로 회절이 `λ/w`로 줄어듭니다.

| 배치 | 경계 폭 |
|---|---|
| 무렌즈, 슬릿 0.2 mm | 10.8 PSU |
| 렌즈 f≈230 mm, 개구 3 mm | **0.80 PSU** |
| 렌즈 f≈230 mm, 개구 5 mm | **0.56 PSU** |

px/PSU 배율은 팔 250 mm 와 같습니다(f 가 팔을 대신함). PSU 환산 경계 폭은 **초점거리와 무관**하고
개구 폭만이 정합니다.

**단, 공짜가 아닙니다.** 개구를 넓히면 그 구간의 표면 기울기 오차가 그대로 각도 오차가 되어
§4의 "평면도는 무관하다"가 무효화됩니다. 요구 평면도는 개구 크기와 무관하게 **λ/16 PV**
(약 37 nm) 로 나옵니다 — 회절 한계와 표면 한계가 같은 1/w 스케일을 따르기 때문입니다.
사출 아크릴이 이걸 만족하는지는 **모릅니다.**

→ 1순위 검증에서 슬릿 폭을 0.2 / 0.5 / 1 / 3 mm 로 바꿔 가며 경계 프로파일을 찍으면
**회절 문제와 평면도 문제가 한 번에 결판납니다.** 슬릿 조각 몇 개면 되므로 비용이 없습니다.
렌즈 도입은 그 결과를 보고 정할 일이지, 지금 선제적으로 설계를 바꿀 일은 아닙니다.

---

## 6. 다음 단계 (우선순위 순)

> ### ★ 2026-09-02 — 목표를 두 단계로 나눴습니다
>
> **1단계: 수동 전자식 굴절 염도계.** 손으로 시료를 올리고 읽는 장치.
> **2단계: 무인 자동화** (플로우셀·펌프·UVC·2점 자가보정 사이클) — 1단계가 되고 나서.
>
> 1단계가 빼는 것: 플로우셀, 페리스탈틱 펌프 4개, UVC 살균, 표준액 보관, 유체 회로,
> 그리고 **"밤에도 뚜껑 덮고 무인으로 돌아야 한다"는 제약**.
> 아래 3순위·4순위는 2단계로 밀립니다.
>
> 1단계에서도 남는 것: 프리즘·광원·검출기·차광·다크 프레임·플랫필드·온도 보정,
> 그리고 **2점 보정은 여전히 필수**입니다(사람이 RO-DI 와 표준액을 올려 주는 것으로 바뀔 뿐).
>
> 1단계는 **핸드 굴절계와 같은 구조가 자연스럽습니다** — 상부 캡이 차광과 광원 홀더를
> 겸하는 형태. 투과형/반사형의 정보량이 동일하다는 것은 §4 에서 계산했습니다.


### 0순위 — 프리즘 주문 (2026-09-01 확정)

**사이언스트리에서 두 세트를 한 번에 — 21,000원.** 동일 스토어라 배송비 한 번.

- 광학 실험용 렌즈 8종 (6,000원) — 반원렌즈 60×20×6 mm, 활꼴, 두께 확정 → **검증용**
- 광학실험용 렌즈와 거울세트 (12,000원) — 반원렌즈 6 cm, 두께 미기재 → **받아서 실측**

근거와 탈락 후보는 [`PRISM_SOURCING.md`](PRISM_SOURCING.md)에 정리했습니다. 요점만:

- 프리즘 재질·AR 코팅·정확한 반원 여부·표면 평면도는 **전부 비임계적**입니다 (2점 보정이 실측)
- **임계적인 것은 두께 하나뿐**입니다. 두께 = 개스킷 랜드 폭. 6 mm는 유로 폭 1~2 mm라 기포 위험
- 아크릴(n=1.491)이 BK7(1.5168)보다 **8% 민감**합니다

### 1순위 — 광학 벤치 가조립 ★ 최대 기술 위험

> **제작 도면은 [`HOBBY_BUILD.md`](HOBBY_BUILD.md) 와 `scad/optical_bench.scad` 에 있습니다.**
> 3D 프린터 + 다이소 재료 전제. 1:1 인쇄 템플릿은 `docs/build.html`.
> 정밀해야 하는 것은 **두 팔이 서로 안 움직이는 것**과 **미광 차단** 둘뿐입니다.

위 반원렌즈 + 590 nm LED + TSL1401로 임시 벤치를 세우고 **그림자 경계가 보이는지** 확인합니다.
측정면에 RO-DI 한 방울, 표준액 한 방울을 번갈아 올려 **경계 픽셀이 30 px 이상 이동**하면 성공입니다.
**경계가 보이는지만 볼 때는 밀봉이 필요 없습니다** — 평면을 위로 놓고 액적만 올리면 됩니다. 두께도 무관합니다.
단 아래 6·7번(증발·이월)은 **실런트를 시공한 실제 헤드**로 해야 의미가 있습니다.

여기서 실패하면 나머지 설계가 전부 무의미하므로, **반드시 이것부터** 하십시오. 유체부·기구부에 돈과 시간을 쓰기 전에 확인해야 합니다.

확인 순서:
1. 공기 중 — 어레이 전체가 밝아짐 (전 각도 전반사, 풀스케일 기준점)
2. RO-DI 한 방울 — 경계선 등장
3. 표준액 한 방울 — 경계선 이동
4. **경계 프로파일을 그대로 저장해 실제 폭(px)과 프레임 간 재현성을 확인** ← 회절 항목 검증
5. **슬릿 폭을 0.2 / 0.5 / 1 / 3 mm 로 바꿔 가며 4번을 반복** ← 회절과 표면 평면도를 한 번에 판정.
   폭이 슬릿에 반비례하면 회절 지배(계산대로), 비례하면 표면 평면도 지배(렌즈 도입 검토)
6. **증발 시험 (2026-09-04 신설)** — 표준액 한 방울을 올리고 뚜껑을 닫은 채
   **10 분 방치한 뒤 다시 읽습니다.** 읽음이 0.05 PSU 넘게 올라가면 증기 웰의
   사면 선접촉이 새는 것입니다. 뚜껑을 슈라우드에서 작은 판으로 바꾸면서 닫는
   힘이 0.7 → 0.2 N 으로 줄었으므로 **이제 이 항목은 계산이 아니라 실측입니다.**
   새면 대안은 보스 사면에 실런트로 성형하는 탄성 립입니다.
7. **이월 시험 (2026-09-04 신설)** — 표준액 → (닦아냄) → RO-DI 를 번갈아 다섯 번
   읽습니다. RO-DI 읽음이 회를 거듭할수록 낮아지면(=처음이 높으면) 어딘가에 액이
   고여 이월되고 있다는 뜻입니다. 실런트 링·주입 슬롯이 제대로 찼는지 보십시오.

**활꼴(60×20)을 쓸 때 배치가 다릅니다** — 두 광학팔 사이각이 127.7°가 아니라 **149.3°**입니다.
`src/segment_optics.py`를 실행하면 형상별 값이 나옵니다.

### 2순위 — 아두이노 스케치 (미작성)

> **★ 2026-09-02 — 오차예산이 펌웨어에 요구사항 다섯 개를 걸었습니다.**
> 이것들 없이는 목표 ±0.12 PSU 가 안 나옵니다 (위 §1 "1단계 오차예산").
>
> 1. **에지 검출이 아니라 프로파일 모델 피팅.** 센트로이드·문턱값은 안 됩니다.
>    실제 신호는 프레넬 곡선이고, 그 모양을 128점에 맞춰 위치를 추정해야
>    잡음 한계 0.004 PSU 와 짧은 팔이 성립합니다.
> 2. **2점 보정을 직선으로 하지 말 것** — `detector_pos()` 가 주는 곡선을 쓰십시오.
>    직선이면 중간 염도에서 0.0837 PSU 틀립니다.
> 3. **플랫필드 나눗셈** — `(측정 − 다크) / 플랫`. 플랫은 EEPROM 에 한 번 저장.
>    잔차 0.2 % 가 목표입니다. 불량화소 검출·보간도 같이 넣으십시오
>    (스팬이 6화소뿐이라 하나만 죽어도 치명적).
> 4. **다크 프레임 규약** — 사이클당 1장, 64프레임 평균, 시작/끝 2장으로 자기검사.
>    적분시간 고정. 액체 교체 직후 첫 2프레임 폐기(이미지 랙 0.5 %).
> 5. **액적 규약 — 부피·타이밍을 고정하고, 온도는 액적마다 읽고,
>    표준액 값은 입력받는다(35 로 박아 두지 말 것).** 아래 §6-1.
>    ★ 이 항목의 실제 최대 위험은 온도가 아니라 **증발**입니다 — 뚜껑을 열어 둔
>    액적은 **분당 0.3~0.9 PSU** 로 농축됩니다. 예산 전체가 0.063 입니다.

### 6-1. 액적 규약 — 온도·증발·표준액 (2026-09-02 확정)

> **2점 보정이 온도를 다 지워 주지 않습니다.** 지워지는 것은 표준액과 시료가
> **함께** 드리프트하는 몫뿐입니다(0.667 → 0.0015 PSU/℃, 450배).
> 둘 **사이의** 차이는 남습니다 — 광학계는 굴절률만 보고, 물리적으로
> **1 ℃ = 0.667 PSU** 라 염도 변화와 구분할 방법이 없습니다.

#### 그런데 액적은 블록 온도에 거의 즉시 붙습니다

20 µL · 0.2 mm 막이면 지름 11.3 mm, 면적 100 mm². 프리즘으로의 전도는
0.30 W/K 이고 열용량은 0.084 J/K 이라 **시상수가 0.28 초**입니다.

> **그래서 "시료는 26 ℃ 수조에서 왔고 보정액은 실온 병에 있다"는 걱정은
> 액적에는 해당되지 않습니다.** 2 초만 기다리면 둘 다 **블록 온도**가 됩니다.
> 남는 것은 두 측정 사이(약 1분)의 **블록 자체의 드리프트**이고, 그것은
> 센서가 읽어서 보정합니다.

#### ★ 진짜 위험은 온도가 아니라 증발입니다

정지 공기·25 ℃·RH 50 % 기준(문헌 0.1~0.3 kg/m²h)으로 20 µL 액적은

| 증발률 | 냉각 depression | 분당 농축 | **분당 염도 상승** |
|---|---|---|---|
| 0.1 kg/m²h | 0.023 ℃ (0.015 PSU) | 0.83 %/min | **0.29 PSU/min** |
| 0.2 | 0.045 ℃ (0.030 PSU) | 1.67 %/min | **0.58 PSU/min** |
| 0.3 | 0.068 ℃ (0.045 PSU) | 2.50 %/min | **0.87 PSU/min** |

**예산 전체가 0.063 PSU 인데 증발은 분당 0.3~0.9 PSU 입니다 — 열 배 이상입니다.**
냉각 depression(0.015~0.045 PSU)도 센서가 못 읽는 값이라, 온도 센서를
0.01 ℃ 급으로 올려도(0.0094) 이쪽이 그대로 남습니다.

> **그래서 순서가 뒤바뀝니다 — 증발이 먼저이고 센서가 나중입니다.**

#### 상쇄 원리 — 똑같이 다루면 증발도 온도도 지워집니다

두 액적이 같은 비율 f 로 농축되면, 표준액의 참값은 S_std(1+f) 인데 펌웨어는
S_std 로 알고 눈금을 만듭니다. 시료도 같은 (1+f) 배로 농축돼 그 눈금을 지나므로
**정확히 상쇄됩니다.** 온도도 같습니다 — 둘 다 블록 온도면 차가 0 입니다.

**남는 것은 두 액적을 서로 다르게 다룬 몫뿐입니다.**

#### 규약

1. **부피를 고정한다.** 같은 스포이드·주사기로 같은 눈금까지. 부피가 다르면
   증발 비율 f 가 달라져 상쇄가 깨집니다.
2. **타이밍을 고정한다.** 액적 올림 → 뚜껑 닫음 → **T_settle 대기** → 읽기.
   보정액과 시료에 **똑같은 T_settle** 을 씁니다. ±5 초 안으로 맞추면
   f 의 차이가 0.08 % 라 0.03 PSU 입니다.
3. **T_settle 은 2 초 이상.** 액적이 블록 온도에 붙는 시상수가 0.28 초입니다.
   길게 잡을수록 증발이 커지므로 **필요한 만큼만** 기다립니다.
4. **뚜껑은 액적을 올린 직후 닫는다.** 증기가 포화되면 증발이 멎습니다.
   ⚠ **뚜껑 안 부피가 작아야 빨리 포화됩니다** — 아래 설계 숙제 참조.
5. **온도는 액적마다 읽는다.** 보정액 1회 + 시료 1회, 한 사이클 최소 2회.
   읽는 자리는 **프리즘 블록**(액적이 붙어 있는 몸통)입니다.
   보정은 두 읽기의 차로: `S_corr = S_meas − (T_sample − T_cal) × 0.667`.
6. **센서의 절대 정확도는 상관없습니다.** 같은 센서·같은 자리의 **차**를
   쓰므로 DS18B20 의 ±0.5 ℃ 오프셋은 상쇄됩니다. 남는 것은 **분해능과
   단기 반복성**이고, 차에는 √2 가 붙어 √2 × 0.0625 × 0.667 = **0.0590 PSU**
   입니다. 이것이 지금 예산의 최대 항목입니다.
7. **표준액 값을 입력받는다 — 35 로 박아 두지 말 것.** 아래 참조.

#### 표준액은 33~36 PSU 로 유동적입니다

병마다 다르고 보관 중에도 변합니다. 2점 보정은 (0, S_std) 로 눈금을 만들므로
**입력값이 틀리면 눈금 전체가 틀립니다.**

| 실제 | 입력 | 34 PSU 시료를 읽으면 | 오차 |
|---|---|---|---|
| 34.5 | 35.0 | 34.49 | **+0.49 PSU** |
| 33.0 | 35.0 | 36.06 | **+2.06 PSU** |

**0.5 PSU 만 틀려도 예산 전체(0.063)의 8배입니다.** 광학·기구의 어떤 개선보다
큽니다. HC-06 시리얼 터미널에서 `SET STD 34.7` 같은 명령으로 **인증값을
입력**하게 하고, EEPROM 에 저장하고, 측정 결과에 **함께 출력**하십시오
(어떤 표준액으로 잰 값인지 나중에 추적할 수 있어야 합니다).

기하는 33~36 전체를 덮습니다 — 조명된 입사각 여유가 0 PSU 쪽 0.305°,
표준액 쪽 0.286°(36 PSU)~0.334°(33 PSU) 로 모두 넉넉합니다. **기구 변경은
필요 없습니다.**

#### 증기 웰 — 2026-09-02 설계 완료

증기가 포화돼야 증발이 멎는데, **덮는 것이 크면 안이 너무 큽니다** (옛 차광 슈라우드가 그랬습니다).

| 뚜껑 안 부피 | 포화에 드는 액적 비율 | 그때의 염도 상승 |
|---|---|---|
| 측정창 웰만 (2.7 cm³) | 0.31 % | 0.11 PSU |
| **옛 슈라우드 전체 (약 300 cm³)** | **34.5 %** | **12 PSU** |

**0.15 mm 틈으로는 안 됩니다** — 둘레 확산만으로 분당 0.4 PSU 가 샙니다.
그래서 **접촉으로 막되, 사면 선접촉**으로 만들었습니다.

- **측정창은 x 로 14 mm** 입니다. 필요한 것은 조명 패치(8.6 mm)와 액적 여유뿐입니다.
  전에는 50 × 17.7(전폭)을 열어 두었습니다.
- **창 y 반폭은 5.8 mm — 프리즘 전폭(10)보다 바깥**입니다 (2026-09-04 개정).
  ⚠ **옛 값 4 는 틀렸습니다.** 프리즘 위에 편측 1 mm 의 살을 남겨 포켓 틈을
  "덮게" 했지만, 포켓이 프리즘을 0.35 떨어뜨려 그 살은 애초에 닿지도 않았고,
  닿았더라도 그 밑 0.05 mm 틈은 모세관압이 +1 kPa 라 액을 **빨아들입니다.**
  지금은 창을 프리즘 전폭까지 열고 옆 틈·살 밑을 실런트로 채워 **액이 닿는 면을
  유리와 굳은 탄성체로만** 만듭니다. 자세한 것은 §4 의 2026-09-04 항목입니다.
  빔 여유도 편측 0.5 → **1.5 mm** 로 넓어졌습니다.
- **창을 25° 사면으로** 팠습니다. 뚜껑 보스는 **12° — 창보다 완만**해서
  아래 모서리만 창 사면에 닿습니다. **선접촉**이라 자기정렬이 되고,
  보스가 **0.8 mm 떠서 액적을 누르지 않습니다.**
- 닫히는 부피가 z 0~0.8 mm 뿐이라 **0.130 cm³**(창을 y 로 넓히면서 0.090 에서
  늘었습니다). 포화에 드는 액적이 **0.015 % (= 0.0052 PSU)** 라 뚜껑을 닫으면
  증발이 사실상 멎습니다.
- 하중은 뚜껑 자중 약 0.2 N 뿐, 블록 처짐은 µm 이하고 보정액·시료에 똑같이
  걸리므로 2점 보정에서 상쇄됩니다. ⚠ 단 **자중이 옛 슈라우드(0.7 N)보다 작아
  선접촉의 밀폐가 인쇄 품질에 더 의존합니다** — 1순위에서 실측하십시오.

**빔의 두께 방향 폭은 7 mm 입니다.** 가장자리가 **마른 프리즘 면**을 때리면 염도와
무관한 배경이 됩니다. 젖는 유리는 프리즘 전폭 10 mm 이므로 편측 1.5 mm 여유입니다
(창을 8 로 두었을 때는 0.5 였습니다). 9.5 에서 7 로 줄이며 광량이 26 % 줄었지만
잡음 항이 0.0026 PSU 라 사실상 무해합니다.

**남은 증발은 액적을 올리고 뚜껑을 닫기까지의 열린 시간뿐**입니다. 그 편차를
±2 초로 보면 0.0193 PSU 입니다.

>    상세는 [`ILLUMINATION.md`](ILLUMINATION.md) §6.

필요한 것: TSL1401 프레임 읽기 → 서브픽셀 에지 검출 → 블루투스 명령 파서 → 시퀀스 상태머신.
명령어 세트와 10단계 시퀀스는 설계 문서 13~14장에 정의되어 있습니다.

주의: SoftwareSerial은 송신 중 인터럽트를 막으므로 TSL1401 프레임 캡처와 겹치지 않게 순차 처리해야 합니다.

### 3순위 — 프리즘 실측 후 STL 설계

클램셸 파라메트릭 모델(OpenSCAD 권장)을 만들려면 **실제 프리즘 치수**가 필요합니다.

**두께가 셀 치수를 결정합니다** (측정면 폭 = 프리즘 두께):

| 두께 | 개구 | 랜드 | 깊이 | 부피 | 판정 |
|---|---|---|---|---|---|
| 6 mm | 2 × 20 mm | 2 mm | 1.5 mm | 60 µl | 기포 위험 높음 |
| 10 mm | 5 × 20 mm | 2.5 mm | 2 mm | 200 µl | **양호** |
| 20 mm | 8 × 30 mm | 6 mm | 2 mm | 480 µl | 여유 |

원설계의 2 mL보다 훨씬 작아지는데, 이월 오차와 플러시 시간 면에서는 **오히려 유리**합니다.
`fig_printed.png`는 25 mm급 프리즘 기준이므로 실측 두께로 다시 잡아야 합니다.

### 4순위 — 유체부

펌프 4개 + MOSFET을 채널 하나씩 검증한 뒤 시퀀스를 붙입니다. **플라이백 다이오드를 먼저 다십시오.**

---

## 7. 인수받는 사람이 흔히 저지를 실수

| 실수 | 결과 |
|---|---|
| TSL1401 모듈의 광각 렌즈를 그대로 둠 | 각도 분포가 뭉개져 **경계선 자체가 안 생김** (2026-09-02 베어 칩 확정으로 해당 없음) |
| 모듈의 `OUT`(신호처리 출력)을 씀 | 고역통과가 DC 계단을 없애고 저역통과가 경계를 한쪽으로 밈. **`AO` 원신호만 쓸 것** |
| 칩 위에 0.1 µF 바이패스를 안 붙임 | 클럭 동기 잡음이 **고정 패턴**으로 실려 없는 경계를 만듦. 평균으로 안 지워짐 |
| 백색 LED 사용 | 번짐 2.0 PSU, 34 ± 2 PSU 창을 덮어 측정 불가 |
| IRF520 MOSFET 모듈 구입 | 로직레벨 아님. 반쯤 켜진 채 발열하다 고장 |
| MOSFET 게이트 풀다운 생략 | 전원 투입·리셋 시 펌프 폭주 → 물난리 |
| HC-06 RXD 직결 | 5 V 인가로 모듈 손상 |
| 플라이백 다이오드 방향 반대 | 전원 투입 즉시 단락 |
| 셀을 비워서 보관 | 염 결정이 측정면에 남음 |
| 프리즘을 접착제로 고정 | 유리에 응력, UV 경화형은 UVC에 열화 |
| 광학 팔을 별도 마운트 2개로 제작 | 서로 움직여 감도를 드리프트로 상실 |
| 슬릿을 더 좁히면 경계가 날카로워질 거라 생각 | **반대입니다.** 회절이 λL/w 로 커집니다 (§4) |
| 경계가 넓어서 못 쓰겠다고 판단 | 폭은 오차가 아닙니다. 2점 보정이 모양을 지웁니다 — 볼 것은 **재현성** (§5) |
| 검출팔을 늘려 분해능을 올리려 함 | 각도 분해능은 안 바뀝니다. 픽셀 샘플링만 좋아집니다 (§4) |
| 온도 센서를 수조에 설치 | 셀 온도와 다름. **셀 유로 안**에 넣어야 함 |

---

## 8. 열려 있는 설계 질문

**조명 배치가 미결정입니다 ★ 2026-09-01 3차 추가.** 슬릿을 렌즈로 측정면에 집광하는지
(배치 A) 렌즈 없이 두는지(배치 B)가 정해지지 않았고, 이것이 **표면 평면도 요구를
정합니다** — 배치 B 는 0.5~2 mm 주기에서 λ/20 급이 필요하고 배치 A 는 요구가 없습니다.
`segment_optics.py` 는 이미 배치 A 를 가정하고 있으나 BOM 에 콘덴서 렌즈가 없었습니다.
전체 내용은 [`ILLUMINATION.md`](ILLUMINATION.md). **1순위 검증에서 조명 패치 폭을
실측하면 결판납니다.**


**검출팔 250 mm의 하우징 크기.** 상자가 약 500 × 150 mm가 됩니다. 부담되면 팔 중간에 평면거울을 넣어 광로를 접으면 절반이 됩니다. 거울 각도 오차는 2점 보정이 흡수하므로 정밀한 거울일 필요는 없습니다. 아직 도면화하지 않았습니다.

**온도 균일화 방식이 두 번 바뀌었습니다.** 처음엔 섬프 침수 → 이후 단열 + 알루미늄 열용량 → 3D 프린팅 대응으로 알루미늄 평판 + 단열 + 코드 보정. 최종안은 `fig_printed.png` 기준이지만 **실측으로 ΔT가 얼마나 남는지 확인이 필요합니다.**

**수조물의 온도 도달.** 수조(26 °C)에서 실온 셀로 들어오는 물이 표준액과 온도가 다를 수 있습니다. 플러시 볼륨을 늘리거나 단열 박스 안에 예열 코일을 넣는 방안이 있으나 아직 정량화하지 않았습니다.

**표준액 소모량.** ~~셀 2 mL × 하루 4회 = 월 240 mL~~ — 이 유도는 플러시 배수를 빼먹었고 셀 용적도 옛값(2 mL)이었습니다.
`design_calc.py` 가 이제 파라메트릭으로 계산합니다: 10배 플러시 × 셀 0.2 mL × 사이클당 1회(보관용 재충전) × 하루 2회 × 30일 = **월 120 mL**.
유휴 시 셀이 표준액으로 차 있으므로 보정점 A 는 공짜이고, 채워 넣는 1회분만 셉니다.
셀 용적이 프리즘 두께로 정해지므로(§6) 실측 후 `CELL_ML` 을 고치면 이 값이 따라 바뀝니다.

---

## 9. 참고자료

- [Index of Refraction of Seawater and Freshwater as a Function of Wavelength and Temperature — Oregon State University, Parrish Research Group](https://research.engr.oregonstate.edu/parrish/index-refraction-seawater-and-freshwater-function-wavelength-and-temperature) — 모든 수치의 근거
- [Digital handheld refractometer — Wikipedia](https://en.wikipedia.org/wiki/Digital_handheld_refractometer) — 임계각·그림자 경계 원리
- [TSL1401CL 데이터시트 — ams-osram (Mouser)](https://www.mouser.com/datasheet/2/588/TSL1401CL-1214741.pdf) · [제품 페이지](https://ams-osram.com/products/photodetectors/photodiodes/ams-tsl1401cl-linear-array-sensor)
- [LED refractometry with Arduino — Arduino Forum](https://forum.arduino.cc/t/led-refractometry-with-arduino/684120) — TCD1304 MHz 샘플링 한계 사례
- [Interfacing Arduino Boards with Optical Sensor Arrays — PMC](https://pmc.ncbi.nlm.nih.gov/articles/PMC10747067/)
- [Biofouling protection for marine environmental sensors — Ocean Science](https://os.copernicus.org/articles/6/503/2010/os-6-503-2010.pdf) · [Antifouling Strategies for Sensors Used in Water Monitoring — Sensors (MDPI)](https://www.mdpi.com/1424-8220/21/2/389)
- [UVC LEDs reduce marine biofouling — Laser Focus World](https://www.laserfocusworld.com/lasers-sources/article/16547014/photonic-frontiers-leds-uvc-leds-reduce-marine-biofouling)
- [The IRF520 FET switching module — arduinodiy](https://arduinodiy.wordpress.com/2020/11/22/the-irf520-fet-switching-module/) · [IRF520 not a good choice for Arduino — Arduino Forum](https://forum.arduino.cc/t/irf520-mosfet-not-a-good-choice-for-arduino-use/702391)
- [HC-05·HC-06 ZS-040 보드 — Martyn Currey](https://www.martyncurrey.com/hc-05-and-hc-06-zs-040-bluetooth-modules-first-look/) · [ProtoSupplies](https://protosupplies.com/product/hc-05-zs-040-bluetooth-module/)
- [ATAGO PAL-06S 사양 (±2‰)](https://novatech-usa.com/Products/Seawater-Salinity/4406-PAL-06S_2.html) — 시판 포켓 굴절계 정확도 비교 기준
- **일체형 모듈 제원의 근거** (§3 "상용 모듈을 쓰지 않는 이유"):
  [Vaisala K-PATENTS Sanitary OEM Refractometer PR-33-AC 데이터시트](https://www.vaisala.com/sites/default/files/documents/PR-33-AC-Sanitary-OEM-Refractometer-B211883EN.pdf)
  (nD 정확도 ±0.0002 · 재현성 ±0.0001 · LED 590 nm · 3648 px CCD · Cargille 표준액 교정) ·
  [Pyxis Prism RT-50 User Manual](https://www.pyxis-lab.com/wp-content/uploads/2023/02/RT-50-User-Manual-v1.03.pdf)
  (±0.2 % Brix, 공기 중 교정, RS-485 로 원시 굴절률 출력)
- [US 7,821,622 — Optical refractometer for measuring seawater salinity and corresponding salinity sensor](https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/7821622) — 해수 염도 전용 광학 굴절계 특허. 선행기술
- [Best Salinity Meters — Bulk Reef Supply](https://www.bulkreefsupply.com/content/post/best-salinity-meters) — 수조 시장의 자동 측정은 전부 전기전도도라는 근거
- [Inline process refractometer — Wikipedia](https://en.wikipedia.org/wiki/Inline_process_refractometer)
- 산업용 인라인 굴절계: [Pyxis RT-50](https://www.pyxis-lab.com/product/rt-50-prism-inline-refractometer/) · [Anton Paar L-Rix](https://www.anton-paar.com/us-en/products/details/l-rix/) · [MISCO IRIS](https://www.misco.com/product/iris-inline-process-refractometer/) · [ATAGO](https://www.atago.net/en/products-prm-top.php)
